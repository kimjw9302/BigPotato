module.exports.function = function getItemNotLocation (locaName, itemName, searchKeyword) {
   var config = require('config');
  var fail = require('fail');
  var http = require('http');
  var console = require('console'); 
  throw fail.checkedError("This error is not found exception", "NotFoundLocation",null);
}
